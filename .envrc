# Activate the Nix devShell defined in flake.nix so `go`, `make`, and other
# toolchain commands are available after `direnv allow`. The Go toolchain (and
# its exact version) is defined once in flake.nix; this just wires direnv to it.
use flake
